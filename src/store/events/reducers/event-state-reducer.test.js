import eventStateReducer, { ACTIONS } from "./event-state-reducer";

test("State ADD NEW REMINDER", () => {
    /*render(<App />);
    const linkElement = screen.getByText(/learn react/i);
    expect(linkElement).toBeInTheDocument();*/
});